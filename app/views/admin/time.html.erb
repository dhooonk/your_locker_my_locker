<div class="container container-fluid">
  <!-- Page Heading -->
      <div class="col-lg-12">
        <div class="card">
        <div class="card-body">
        <h3 class="page-header">
          사물함 설정 <small>Locker Setup</small>
        </h3>
        <hr>
        <%= flash[:notice] %>
        <%= flash[:caution] %>
        <ol class="breadcrumb">
            <li class="active">
              신청 받을 사물함에 대한 기간 설정을 할 수 있습니다.<br>
              띄어쓰기를 주의하여 16자에 맞춰서 작성해 주세요.<br>
              양식에 맞춰서 작성하지 않을 시 초기 값으로 설정됩니다.
            </li>
        </ol>

        <h4>사물함기간 설정</h4>
        <hr>
        <div class="col-lg-12">
          <%= form_tag admin_time_path(@limit_id), method: "patch" do %>
            <div class="table-responsive">
               <table class="table">
                 <tr>
                   <td><strong>학부생 신청기간</strong></td>
                   <td>
                     [시작]:
                   </td>
                       <td>
                         <input type="text" class="form-control" name="studentTimeStart" placeholder="2000-00-00 15:00">
                       </td>
                     <td>
                       [종료]:
                     </td>
                     <td>
                       <input type="text" class="form-control" name="studentTimeEnd" placeholder="2000-00-00 15:00">
                   </td>
                   <td>
                     [현재]:
                   </td>
                   <td>
                   <%= @limit.studentTimeStart.chomp("+0900") %>~ <%= @limit.studentTimeEnd.chomp("+0900") %>
                   </td>
                 </tr>

               </table>
               <input type="submit" class="btn btn-dark" value="설정완료" onclick="return confirm('입력하신 설정기간으로 변경하시겠습니까?')">
             </div>
          <% end %>
            </div>
          </div>
        </div>
      </div>
</div>
